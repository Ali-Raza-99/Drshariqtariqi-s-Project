# Dr. Shariq Tariqi’s Patients’ and Mureeds’ Portal

A React web application built with Vite and Material UI.

## Tech

- React + Vite
- Material UI
- Firebase Auth
- Firebase Firestore

## Development

- Install: `npm install`
- Run dev server: `npm run dev`
- Build: `npm run build`
